import Button from "./Button"

const Header = () => {
  return (
    <header className="header">
        <h1>TASK TRACKER</h1>
        <Button color="green" text="ADD"/>
    </header>
  )
}

export default Header